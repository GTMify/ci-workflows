# Paths this repo's secret scan deliberately does not read. One path per line,
# reason after "#". Keep this file short: every line here is a place a real
# credential could hide, so a broad entry costs more than it saves.
#
# scripts/secret_scan_gate.sh is deliberately NOT listed. Its detector comments
# were written to describe key shapes in prose rather than to quote them, so the
# gate still scans its own source. GTM-1590.

tests/secret_scan_gate_test.sh  # A scanner's test suite must contain strings matching every shape the scanner detects, including a PEM private key header, or the tests prove nothing. All values in it are synthetic. GTM-1590.
